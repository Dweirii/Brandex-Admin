# استكشاف أخطاء البريد الإلكتروني

## المشكلة: عدم وصول البريد الإلكتروني للمدير

### الخطوة 1: اختبار الوظيفة الأساسية

1. **افتح صفحة الاختبار**:
   ```
   http://localhost:3001/test-email
   ```

2. **اضغط على زر "Test Email Functionality"**

3. **تحقق من النتائج في الصفحة**

4. **افتح Developer Tools (F12) وتحقق من Console**

### الخطوة 2: التحقق من المتغيرات البيئية

تأكد من وجود هذه المتغيرات في ملف `.env.local`:

```env
# Resend API Key
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# Admin Email (اختياري)
ADMIN_EMAIL=zaid@dweiri.dev

# Admin Dashboard URL (اختياري)
ADMIN_DASHBOARD_URL=http://localhost:3001
```

### الخطوة 3: التحقق من إعدادات Resend

1. **تسجيل الدخول إلى [resend.com](https://resend.com)**

2. **تحقق من API Key**:
   - اذهب إلى Settings > API Keys
   - تأكد من أن المفتاح صحيح ومفعل

3. **تحقق من النطاق**:
   - اذهب إلى Settings > Domains
   - تأكد من إضافة نطاقك والتحقق منه
   - أو استخدم `noreply@resend.dev` للاختبار

4. **تحقق من Activity Log**:
   - اذهب إلى Activity
   - تحقق من محاولات إرسال البريد الإلكتروني
   - تحقق من الأخطاء إن وجدت

### الخطوة 4: تشخيص الأخطاء الشائعة

#### خطأ: "Invalid API Key"
- **الحل**: تحقق من صحة `RESEND_API_KEY`
- **الحل**: تأكد من نسخ المفتاح كاملاً

#### خطأ: "Domain not verified"
- **الحل**: استخدم `noreply@resend.dev` للاختبار
- **الحل**: أضف نطاقك إلى Resend وتحقق منه

#### خطأ: "Rate limit exceeded"
- **الحل**: انتظر قليلاً وحاول مرة أخرى
- **الحل**: ترقية خطة Resend إذا لزم الأمر

#### خطأ: "Invalid email address"
- **الحل**: تحقق من صحة بريد إلكتروني المدير
- **الحل**: تأكد من تنسيق البريد الإلكتروني

### الخطوة 5: اختبار Webhook

1. **استخدم Stripe CLI لاختبار Webhook**:
   ```bash
   stripe listen --forward-to localhost:3001/api/webhook
   ```

2. **تحقق من سجلات Webhook في Console**

3. **تأكد من أن Webhook يصل إلى النقطة الصحيحة**

### الخطوة 6: تشخيص متقدم

#### تحقق من سجلات التطبيق:
```bash
npm run dev
```
ثم تحقق من Terminal للحصول على سجلات مفصلة.

#### تحقق من Network Tab:
1. افتح Developer Tools
2. اذهب إلى Network tab
3. اضغط على زر الاختبار
4. تحقق من طلب `/api/test-email`

#### تحقق من Console:
ابحث عن رسائل التشخيص التي تبدأ بـ:
- 🔄 (بداية العملية)
- ✅ (نجح)
- ❌ (فشل)
- 📧 (معلومات البريد الإلكتروني)

### الخطوة 7: حلول بديلة

#### إذا لم يعمل Resend:
1. **جرب خدمة أخرى مثل SendGrid أو Nodemailer**
2. **استخدم Gmail SMTP للاختبار**

#### إذا لم يصل Webhook:
1. **تحقق من إعدادات Stripe Webhook**
2. **تأكد من صحة `STRIPE_WEBHOOK_SECRET`**
3. **تحقق من أن Webhook URL صحيح**

### الخطوة 8: اختبار يدوي

يمكنك اختبار إرسال البريد الإلكتروني يدوياً:

```bash
curl -X POST http://localhost:3001/api/test-email \
  -H "Content-Type: application/json"
```

### رسائل التشخيص المتوقعة

#### عند النجاح:
```
🔄 Attempting to send admin notification email...
📧 Admin email: zaid@dweiri.dev
📧 Resend API Key exists: true
✅ Admin email sent successfully: { id: "email_id" }
✅ Email ID: email_id
```

#### عند الفشل:
```
❌ Error sending email to admin: { error details }
❌ Error details: { full error object }
```

### الاتصال بالدعم

إذا استمرت المشكلة:
1. **احفظ سجلات Console كاملة**
2. **التقط لقطة شاشة من صفحة الاختبار**
3. **شارك رسائل الخطأ من Resend Dashboard**
4. **تحقق من إعدادات Firewall/Network**

### ملاحظات مهمة

- تأكد من أن التطبيق يعمل على `localhost:3001`
- تحقق من أن جميع المتغيرات البيئية موجودة
- تأكد من أن Resend API Key صحيح ومفعل
- تحقق من مجلد Spam في بريد إلكتروني المدير 